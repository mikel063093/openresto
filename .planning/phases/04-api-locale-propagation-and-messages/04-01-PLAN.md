---
phase: 04-api-locale-propagation-and-messages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openresto-frontend/api/client.ts
  - openresto-frontend/app/(user)/booking-confirmation/[bookingRef].tsx
  - openresto-frontend/tests/api/client.test.ts
  - openresto-frontend/tests/api/auth.test.ts
  - openresto-frontend/tests/api/bookings.test.ts
  - openresto-frontend/tests/api/holds.test.ts
  - .planning/phases/04-api-locale-propagation-and-messages/04-01-SUMMARY.md
autonomous: true
requirements:
  - PUB-03
user_setup: []
must_haves:
  truths:
    - "Frontend API requests set `Accept-Language` centrally from the active `en` or `es-CO` locale."
    - "Per-screen or ad hoc locale headers are removed from migrated frontend API surfaces."
  artifacts:
    - "openresto-frontend/api/client.ts"
    - "openresto-frontend/tests/api/client.test.ts"
    - "openresto-frontend/app/(user)/booking-confirmation/[bookingRef].tsx"
  key_links:
    - "Frontend transport uses the same persisted locale contract as the product UI."
---

<objective>
Centralize frontend locale propagation in shared API helpers.

Purpose: Make the frontend transport layer responsible for `Accept-Language` so screen code no longer hand-builds locale headers.
Output: Updated shared API client, removal of ad hoc locale headers, and focused frontend API tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-api-locale-propagation-and-messages/04-CONTEXT.md
@openresto-frontend/api/client.ts
@openresto-frontend/i18n/locale.ts
@openresto-frontend/services/storage.ts
@openresto-frontend/app/(user)/booking-confirmation/[bookingRef].tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Resolve the active locale centrally in the shared frontend API client</name>
  <files>openresto-frontend/api/client.ts, openresto-frontend/tests/api/client.test.ts</files>
  <read_first>openresto-frontend/api/client.ts, openresto-frontend/i18n/locale.ts, openresto-frontend/services/storage.ts</read_first>
  <action>Teach the shared API client to derive `Accept-Language` from the persisted active locale with browser-aware fallback, and set that header automatically on outgoing requests while preserving existing body/credential behavior.</action>
  <verify>cd openresto-frontend && npm test -- --runInBand tests/api/client.test.ts</verify>
  <acceptance_criteria>
    - "Shared API requests carry `Accept-Language: en` or `Accept-Language: es-CO` automatically."
    - "Existing request shapes, credentials defaults, and custom-header support still work."
  </acceptance_criteria>
  <done>Frontend shared transport owns locale propagation.</done>
</task>

<task type="auto">
  <name>Task 2: Remove per-screen locale header handling and prove downstream API usage still works</name>
  <files>openresto-frontend/app/(user)/booking-confirmation/[bookingRef].tsx, openresto-frontend/tests/api/auth.test.ts, openresto-frontend/tests/api/bookings.test.ts, openresto-frontend/tests/api/holds.test.ts</files>
  <read_first>openresto-frontend/app/(user)/booking-confirmation/[bookingRef].tsx and the existing API tests</read_first>
  <action>Delete the known ad hoc route-level `Accept-Language` header and extend focused API consumer tests to prove the shared client keeps auth, booking, and hold calls functioning under centralized locale propagation.</action>
  <verify>cd openresto-frontend && npm test -- --runInBand tests/api/auth.test.ts tests/api/bookings.test.ts tests/api/holds.test.ts</verify>
  <acceptance_criteria>
    - "No migrated frontend screen sets its own `Accept-Language` header."
    - "Targeted API consumer tests continue passing with the shared client locale behavior."
  </acceptance_criteria>
  <done>Ad hoc locale headers are removed and frontend API consumers remain stable.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted frontend API Jest suites pass.
- [ ] Shared transport sends the active locale centrally.
- [ ] No migrated screen keeps a hard-coded locale header.
</verification>

<success_criteria>
- All tasks completed
- Frontend locale propagation is centralized
- Ad hoc request-language handling is removed from migrated screens
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-locale-propagation-and-messages/04-01-SUMMARY.md`
</output>
