---
phase: 04-api-locale-propagation-and-messages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openresto-frontend/api/client.ts
  - openresto-frontend/api/admin.ts
  - openresto-frontend/api/restaurants.ts
  - openresto-frontend/tests/api/client.test.ts
  - openresto-frontend/tests/api/admin.test.ts
  - openresto-frontend/tests/api/restaurants.test.ts
  - .planning/phases/04-api-locale-propagation-and-messages/04-01-SUMMARY.md
autonomous: true
requirements:
  - PUB-03
user_setup: []
must_haves:
  truths:
    - "Frontend API helpers send `Accept-Language` centrally from the active locale."
    - "Internal upload/delete API callers use the same locale-propagation path as JSON helpers."
  artifacts:
    - "openresto-frontend/api/client.ts"
    - "openresto-frontend/api/admin.ts"
    - "openresto-frontend/api/restaurants.ts"
  key_links:
    - "Locale transport is centralized instead of repeated in individual screens or API modules."
---

<objective>
Centralize frontend locale propagation in shared API helpers.

Purpose: make shared API transport responsible for `Accept-Language` so frontend callers stop owning locale headers individually.
Output: updated frontend API client/helpers plus focused API tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-api-locale-propagation-and-messages/04-CONTEXT.md
@openresto-frontend/api/client.ts
@openresto-frontend/api/admin.ts
@openresto-frontend/api/restaurants.ts
@openresto-frontend/context/I18nContext.tsx
@openresto-frontend/i18n/locale.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend shared API transport with locale-aware headers</name>
  <files>openresto-frontend/api/client.ts, openresto-frontend/tests/api/client.test.ts</files>
  <read_first>openresto-frontend/context/I18nContext.tsx, openresto-frontend/i18n/locale.ts, openresto-frontend/services/storage.ts</read_first>
  <action>Add shared request-language detection/header helpers to the frontend API layer so standard JSON requests automatically send `Accept-Language` from the active locale with English fallback.</action>
  <verify>cd openresto-frontend && npm test -- --runInBand tests/api/client.test.ts</verify>
  <acceptance_criteria>
    - "Shared API calls include `Accept-Language` without per-call header wiring."
    - "Stored/browser Spanish locale normalizes to `es-CO` for transport."
  </acceptance_criteria>
  <done>Locale-aware shared API transport is in place and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Route upload/delete callers through the same shared locale transport</name>
  <files>openresto-frontend/api/admin.ts, openresto-frontend/api/restaurants.ts, openresto-frontend/tests/api/admin.test.ts, openresto-frontend/tests/api/restaurants.test.ts</files>
  <read_first>openresto-frontend/api/admin.ts, openresto-frontend/api/restaurants.ts</read_first>
  <action>Update direct internal API `fetch()` callers to reuse the shared locale-aware transport path so multipart upload/delete requests preserve credentials and inherit `Accept-Language` centrally.</action>
  <verify>cd openresto-frontend && npm test -- --runInBand tests/api/admin.test.ts tests/api/restaurants.test.ts</verify>
  <acceptance_criteria>
    - "Internal upload/delete API calls inherit centralized locale propagation."
    - "Existing request shapes and credentials behavior remain unchanged."
  </acceptance_criteria>
  <done>All targeted frontend API transport surfaces share one locale-propagation path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Frontend API client tests pass.
- [ ] Upload/delete API tests pass.
- [ ] No request body shapes or credentials defaults regress.
</verification>

<success_criteria>
- All tasks completed
- Shared frontend API transport owns locale propagation
- Ad hoc internal API header handling is removed from the targeted surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-locale-propagation-and-messages/04-01-SUMMARY.md`
</output>
