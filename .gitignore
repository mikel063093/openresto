# Root .gitignore for the repository

# Node / frontend
node_modules/
openresto-frontend/.expo/
openresto-frontend/dist/
openresto-frontend/web-build/

# .NET build outputs
**/bin/
**/obj/
*.dll
*.pdb
*.exe
*.nuget.props
*.nuget.g.targets

# VS / Rider
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates
.idea/

# OS files
.DS_Store
Thumbs.db

# env files
*.env
.env*

# Local secrets (never commit)
**/appsettings.Local.json

# Claude Code local settings
.claude/settings.local.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
**/docker-compose.override.yml

# Coverage
CoverageReport/
coverage/
**/coverage*.xml
**/TestResults/
**/coverage-report*/
.claude/settings.json
docs/superpowers/plans/2026-05-24-highlights-tags.md
# Uploaded media files
OpenRestoApi/wwwroot/media/

# VPS runtime files (live DB, SSL certs/keys, deploy scripts, logs)
data/
nginx/ssl/
auto_update.sh
update.log

# Local SQLite dev DB + WAL/SHM sidecars. Never commit runtime DB files:
# they change on every transaction, bloat the repo, and the -wal/-shm sidecars
# are only valid alongside their main .db file.
*.db
*.db-shm
*.db-wal
*.db-journal
# Playwright MCP plugin snapshots
.playwright-mcp/
# Local debug screenshots (page captures, before/after diffs)
bookings-table-*.png
page-*.png
# Session handoff notes
.remember/
docs/*
# Track implementation plans and user-facing docs explicitly
!docs/plans/
!docs/plans/**
!docs/backup-restore.md
!docs/screenshots/

#Claude
.claude/investigations

# ZCode local session data
.zcode/
